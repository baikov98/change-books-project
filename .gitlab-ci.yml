stages:
  - build
  - deploy
  
build in gitlab:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$CI_REGISTRY_USER" -p "$CI_REGISTRY_PASSWORD" $CI_REGISTRY
  script:
    - docker build -f .docker/Dockerfile --pull -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA" -t "$CI_REGISTRY_IMAGE:latest" .
    - docker push "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA"
    - docker push "$CI_REGISTRY_IMAGE:latest"
  only:
    - develop
    - test
    - master
    - devops
  when: manual
  tags:
    - docker

build in dockerhub:
  image: docker:latest
  stage: build
  services:
    - docker:dind
  before_script:
    - docker login -u "$DOCKER_HUB_USER" -p "$DOCKER_HUB_PASSWORD" $DOCKER_HUB_REGISTRY
  script:
    - docker build -f .docker/Dockerfile --pull -t "$DOCKER_HUB_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA" -t "$DOCKER_HUB_REGISTRY_IMAGE:latest" .
    - docker push "$DOCKER_HUB_REGISTRY_IMAGE:$CI_COMMIT_SHORT_SHA"
    - docker push "$DOCKER_HUB_REGISTRY_IMAGE:latest"
  only:
    - develop
    - test
    - master
    - devops
  when: manual
  tags:
    - docker

deploy-to-review:
  image: docker:latest
  stage: deploy
#  before_script:
#    - eval $(ssh-agent -s)
#    - echo "$SSH_PRIVATE_KEY"  | tr -d '\r' | ssh-add -
#    - echo "$SSH_PRIVATE_KEY" | ssh-add -
  script:
    - eval $(ssh-agent -s)
    - echo "$SSH_PRIVATE_KEY" | tr -d '\r' | ssh-add -
#    - echo "$SSH_PRIVATE_KEY" | ssh-add -
#    - echo "$SSH_PRIVATE_KEY"  | tr -d '\r' | ssh-add -
    - scp ./.docker/docker-compose-standing.yml student@books-exchange-dev.spring-intensive-2021.simbirsoft1.com:~/docker
    - ssh student@books-exchange-dev.spring-intensive-2021.simbirsoft1.com 'bash -s' < ./.docker/deploy.sh
  environment:
    name: devops/$CI_COMMIT_REF_NAME
  only:
    - develop
    - test
    - master
    - devops
  when: manual
  tags:
    - docker

